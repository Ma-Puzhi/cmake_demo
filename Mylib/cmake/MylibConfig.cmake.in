@PACKAGE_INIT@
# 包含导出的 targets
include("${CMAKE_CURRENT_LIST_DIR}/MylibTargets.cmake")
# 如果你的库还依赖别的包（例如 Eigen），可以写：
# find_dependency(Eigen3 REQUIRED)
# 设置一些路径变量（可选）是因为在老的CMake项目中，一般通过find
# set(MyLib_INCLUDE_DIRS "@PACKAGE_INCLUDE_INSTALL_DIR@")
